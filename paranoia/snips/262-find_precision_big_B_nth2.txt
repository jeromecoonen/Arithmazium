    big_b = ONE
    precision = ZERO
    doit = True
    while doit:
        precision = precision + ONE
        big_b = big_b * b
        y = big_b + ONE
        doit = (y - big_b) == ONE
    return precision, big_b


